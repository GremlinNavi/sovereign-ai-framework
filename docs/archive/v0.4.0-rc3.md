# v0.4.0-rc3 archive record

## Status

This is a public-safe source-input and pre-release import record for `v0.4.0-rc3`
(`0.4.0rc3` as the Python package version). It is not evidence of a previously tagged
hosted release, executable, or release asset. A later tagged release record must add
the exact host references and hashes from the final reviewed artifacts.

## Supplied source-input evidence

The following local source ZIP was inspected on 2026-09-01 before the sanitized
source import:

- filename: `sovereign-ai-demonstrator-eternal-thread-v0.4.0-rc3.zip`
- SHA-256: `B87DFF384D5680F415E500BB596C5D73A68B4D7EBE55CB6509534E4C73CEC547`
- non-directory entries: 58

Its manifest contained only the expected source and public-release-material paths.
The only data-root entries were intentional `.gitkeep` placeholders and
`.env.example`; no real `.env`, session, audit-log, database, training-data, or
credential path was included. This ZIP is retained input evidence only and is not a
final release asset checksum.

## Separate retained-base declaration

[PROVENANCE.md](../../PROVENANCE.md) records a differently named, maintainer-declared
retained base archive and SHA-256. That archive was not available for independent
verification in this workspace. Its filename and hash differ from the supplied ZIP
above, so no relationship between the two is asserted here. Treat them as separate
records until the maintainer verifies their provenance.

## Sanitized import boundary

The source import excludes the ZIP itself and all local or generated material that
could contain private data, including:

- conversation, audit, safety, knowledge, training, and review data;
- `.env` files other than the public `.env.example` template;
- credentials, private keys, certificates, SQLite indexes, and local exports;
- virtual environments, Python caches, test caches, and build output; and
- local professionalism-audit outputs and generated PyInstaller `.spec` files.

The tracked data directories retain only explicit `.gitkeep` placeholders where
needed to document the expected local layout.

## Verification recorded for this import

On 2026-09-01, the full Python test suite was run with an isolated disposable data
directory and pytest base directory: **47 passed**. This is source-test evidence only;
it is not a live model, Ollama, security-penetration, Windows-build, or production
deployment test.

## Hosted source-import references

The initial sanitized source-import commits were pushed on 2026-09-01:

- GitHub branch `release/v0.4.0-rc3-source`:
  `79d2161d556f8fbe88c3c1c87fce004f8f13c925`
- GitLab branch `release/v0.4.0-rc3-source`:
  `aa8ab691f1fc51006b1570f1bc5f40f10367601d`

Those commits have different host-specific IDs but the same Git tree: 75 paths with
identical file paths and blob IDs. This is a source-import record, not a tag or a
release-asset manifest. The GitHub source branch is submitted as a draft pull request
and the GitLab source branch as a draft merge request for review.

## Hosted release fields to complete later

Before creating or announcing a hosted release, record the exact GitHub and GitLab
branch/tag references, full commit IDs, final source/executable filenames, SHA-256
values, checksum-manifest hash, SBOM/notices provenance, and final asset smoke-test
results. Until then, do not claim that `v0.4.0-rc3` has a hosted release asset.
