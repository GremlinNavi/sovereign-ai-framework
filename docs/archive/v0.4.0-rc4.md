# v0.4.0-rc4 archive record

This is a public-safe source-candidate record for `v0.4.0-rc4` (`0.4.0rc4` as the
Python package version). It is not evidence of a hosted release, tag, source ZIP,
Windows executable, or checksum manifest.

## Candidate scope

RC4 adds an opt-in Windows PowerShell bootstrap and guarded Git update workflow, with
documentation that preserves the project's local-first, configurable-backend
boundary. It does not bundle or install an inference runtime, download model weights,
change a user's global PowerShell policy, or add a cloud fallback.

The scripts and documentation are source changes, not proof that a Windows build,
Steam Deck build, or any third-party runtime has been tested or released.

## Privacy exclusions

No conversation histories, audit/safety logs, knowledge files or indexes,
training/review data, `.env` files, credentials, private keys, user exports,
machine-specific virtual environments, local release artifacts, or local paths
belong in this record or in a public source release.

## Initial source-candidate references and verification

The initial RC4 source candidate was published for review on 2026-09-01. These are
host-specific immutable commits, not a release tag:

- GitHub review branch `release/v0.4.0-rc4-source`:
  [`d2d6406ca73ab9b1833e9c6c0731e8bafe939a79`](https://github.com/GremlinNavi/sovereign-ai-framework/commit/d2d6406ca73ab9b1833e9c6c0731e8bafe939a79)
- GitLab review branch `release/v0.4.0-rc4-source`:
  [`a49768ffe45d0ca6c33edbd1dc52e551f03862a0`](https://gitlab.com/eternal-thread-group/sovereign-ai-framework/-/commit/a49768ffe45d0ca6c33edbd1dc52e551f03862a0)
- Cross-host tree verification: both initial candidates contain the same 95
  path/mode/object-ID entries (81 blobs and 14 trees), with no missing or differing
  entry. The GitHub root tree is
  `c41f0f3bf634091e95e735b3f835ac6020ed7816`; it was matched against GitLab's
  complete recursive tree.

Before the archival-record update, the reviewed source candidate completed these
local checks on 2026-09-01:

- Python compilation: `python -m compileall -q app tests config.py launcher.py`
  (passed).
- Configuration validation: `python config.py --validate` (passed).
- Test suite in a project virtual environment with an isolated temporary directory:
  `python -m pytest -q --basetemp <isolated temporary directory>` (52 passed).
- PowerShell parser and `-WhatIf` checks for the new bootstrap and updater under
  Windows PowerShell 5.1 and PowerShell 7+ (passed; no working-tree change).
- `./tools/Test-PublicReleaseReadiness.ps1 -Version v0.4.0-rc4 -RequireClean`
  against both staged Git trees (exit code 2 only for the expected review warnings:
  no `v0.4.0-rc4` tag and no release asset/checksum pair).

This archival-record update is documentation-only. The initial source-candidate
commits above are the tested source references; no hosted release is implied.

## Not created

- Hosted tag: `[NOT CREATED]`
- Release assets, checksum manifest, and RC4 asset SHA-256 values: `[NOT CREATED]`

GitHub is the canonical public source and GitLab is a maintainer-controlled archival
mirror. Their commit IDs may differ because each host has independent history; a
matching reviewed source tree is the relevant comparison. Do not force-push either
host merely to make commit IDs match.
