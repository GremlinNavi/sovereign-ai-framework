# OSWAP AI エンドポイント仕様

[English](OSWAP_AI_ENDPOINTS.md) · [Français canadien](OSWAP_AI_ENDPOINTS.fr-CA.md) · 日本語

## 状態

OSWAP は、将来のインフラ向けに `oswap.ca`、`oswap.jp`、`oswap.us` を登録しています。

本書は、これらのドメインまたは OSWAP サブドメインを、現在稼働中の Web サイト、Git エンドポイント、API、その他の公開サービスとして扱いません。ドメイン登録とサービス提供開始は別の状態です。

ここで説明する AI サブドメインは、DNS、TLS、エッジ・ルーティング、ホスティング、Git プロトコル処理が各エンドポイントについて実際に導入・検証されるまでは設計目標です。

それまでは、現在のインストール手順やリポジトリ手順では、OSWAP 所有ドメインではなく、検証済みの GitHub または GitLab の URL を使用します。

## 目的

OSWAP AI Demonstrator は将来的に、特定の Git フォージの恒久利用に依存せず、OSWAP が管理する安定した公開アドレスから利用できることを目標とします。

計画中のピア・ホスト名は次のとおりです。

- `ai.oswap.ca` — 計画中。現在オンラインとは表現しません
- `ai.oswap.jp` — 計画中。現在オンラインとは表現しません
- `ai.oswap.us` — 計画中。現在オンラインとは表現しません

いずれか一つを国別の主系または正規系として指定しません。導入と検証が完了した後、独立して到達可能な OSWAP ドメインを通じて同じプロジェクト・アイデンティティを表すことを想定します。

## ブラウザーでの動作

将来導入されたエンドポイントへ通常のブラウザーからアクセスした場合、OSWAP AI Demonstrator の人間向けプロジェクトページを返すことを想定します。ページには、プロジェクトの状態、ドキュメント、ソースの所在、リリース情報、ライセンス、整合性情報を含めます。

## Git での動作

計画中のホスト名は、導入と検証の完了後、読み取り専用の Git Smart HTTP を提供することを想定します。

意図的に非稼働ホスト名を使うドキュメントやテストでは、実在する OSWAP 所有ドメインではなく、予約済みの例示用ドメインまたは `.invalid` を使用するべきです。例:

```text
git clone https://ai.oswap.invalid
git pull https://ai.oswap.invalid main
```

`.invalid` は、明らかに非稼働であることを示す用途のために予約されています。上記のコマンドは構文例であり、名前解決されることを想定していません。

OSWAP インフラが実際に導入され、独立に検証された後、運用向けドキュメントで適切な実稼働ホスト名へ置き換えることができます。

ブランチ引数は通常の Git refspec です。ここでは現在の既定ブランチが `main` であるため例として使用しています。

## 初期プロトコル範囲

最初の公開実装は読み取り専用とします。

必要な Git サービス:

- clone、fetch、pull のための `git-upload-pack`

初期の公開エンドポイントでは提供しないもの:

- 一般公開または未認証の push のための `git-receive-pack`

コントリビューターの書き込みアクセスは、将来、明示的な認証を備えた専用の書き込みエンドポイントを設計しない限り、認証済みのリポジトリ・ホスト上のワークフローを使用します。

## Git フォージからの独立性

GitHub と GitLab は現在の公開・共同作業ホストです。計画中の OSWAP 所有ドメインは将来の公開アイデンティティであり、現時点でそれらのフォージ URL を置き換える稼働中サービスではありません。

将来想定する関係は次のとおりです。

```text
ai.oswap.ca / ai.oswap.jp / ai.oswap.us
        [計画中・現在オンラインではない]
                    ↓
OSWAP が管理するプロジェクト・アイデンティティ
                    ↓
Git 転送 / リポジトリ・ルーティング
                    ↓
GitHub、GitLab、または他の互換バックエンド
```

将来的にはバックエンドを変更しても、利用者に案内する OSWAP 公開アドレスを変更しなくてよい設計を目指します。

## ピア・ドメイン原則

計画中の `.ca`、`.jp`、`.us` エンドポイントは、主系とミラーの階層ではなくピアです。将来、異なるインフラを経由しつつ同等のプロジェクト状態を提示する場合があります。

検証していない同期保証を公称してはいけません。公開する整合性情報には、必要に応じてコミット SHA とリリースのチェックサムを含めます。

親ドメインを所有していること自体は、特定のドメインやサブドメインが稼働中であること、または別のエンドポイントと Git 状態が同一であることの証明にはなりません。DNS 委任、TLS、アプリケーション・ルーティング、Git の挙動、コンテンツ同等性は別々に検証する必要があります。

## Order of Operations によるリポジトリ・アドレッシング

OSWAP Twin の設計では、数式で指定するリポジトリ・アイデンティティも検討しています。`9/3` のような正規の算術識別子は、DNS などで安全な表現が必要な場合に `9d3` のようなラベルへ符号化できます。

登録済みドメイン上で将来使用する候補名の例:

```text
repo9d3.oswap.ca   [計画中]
repo9d3.oswap.jp   [計画中]
repo9d3.oswap.us   [計画中]
```

将来の PowerShell/Git ラッパーのパーサー説明やテストでは、次のような非稼働名を使えます。

```text
git pull repo(9/3).oswap.invalid
```

その後、導入プロファイルが明示的に指定した場合に限り、検証済みの式を実際の運用ホスト名へ対応付けます。

これらは設計上の例であり、現在サブドメインやラッパー構文が稼働しているという意味ではありません。

## 命名

- 取り組み: Open-Source World Access Project (OSWAP)
- AI プロジェクト: OSWAP AI Demonstrator
- リポジトリ・スラッグ: `oswap-ai-demonstrator`
- 将来のインフラ用に登録済みの OSWAP ドメイン: `oswap.ca`、`oswap.jp`、`oswap.us`
- 計画中の公開 AI ホスト名: `ai.oswap.ca`、`ai.oswap.jp`、`ai.oswap.us`
- 現在の OSWAP Web サイト状態: このプロジェクトでは未導入

エンドポイント名は OSWAP AI Demonstrator の改名ではありません。OSWAP が将来管理する予定のアドレスを示します。

## 導入原則

ブラウザー向けコンテンツと Git プロトコル通信は将来同じホスト名を共有する可能性がありますが、別のリクエスト種別として扱います。

ドメインまたはサブドメインは、関連する DNS、TLS、ホスティング/ルーティング、アプリケーションまたは Git の動作が外部クライアントから検証されるまで、稼働中と記載してはいけません。

本書は予定される将来仕様のみを定義します。OSWAP ドメイン、AI サブドメイン、数式アドレス型リポジトリ・サブドメインが現在オンラインであるとは主張しません。
